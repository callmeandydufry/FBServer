/*$T Common/Messages/SSHandShake.h GC 1.140 10/10/07 10:06:39 */


/*$6
 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 */


#ifndef __SSCONNECT_H__
#define __SSCONNECT_H__

#include "TypeDefine.h"
#include "KwNetMessage.h"
#include "MessageFactory.h"

DECLARE_NET_MESSAGE(SSHandShake, MESSAGE_SS_CONNECT)

/*
 =======================================================================================================================
 =======================================================================================================================
 */
SSHandShake()
{
	m_MapID = 0;
	m_MainVersion = 0;
	m_SubVersion = 0;
	m_InnerVersion = 0;
	m_Revision = 0;
};

/*
 =======================================================================================================================
 =======================================================================================================================
 */
virtual ~SSHandShake()
{
};

/*
 =======================================================================================================================
 =======================================================================================================================
 */

virtual uint32 GetMsgSize() const
{
	return sizeof(ID_t) * 5;
}

public:

/*
 =======================================================================================================================
 =======================================================================================================================
 */
ID_t GetMapID()
{
	return m_MapID;
};

/*
 =======================================================================================================================
 =======================================================================================================================
 */
void SetMapID(ID_t id)
{
	m_MapID = id;
};
private:
	ID_t	m_MapID;
	ID_t	m_MainVersion;
	ID_t	m_SubVersion;
	ID_t	m_InnerVersion;
	ID_t	m_Revision;

DECLARE_NET_MESSAGE_FACTORY(SSHandShake, MESSAGE_SS_CONNECT)

/*
 =======================================================================================================================
 =======================================================================================================================
 */
uint32 GetMessageMaxSize() const
{
	return sizeof(ID_t) * 5;
}

DECLARE_NET_MESSAGE_HANDLE(SSHandShake, MESSAGE_SS_CONNECT)
#endif
